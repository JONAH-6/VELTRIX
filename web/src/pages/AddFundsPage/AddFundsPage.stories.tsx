import type { Meta, StoryObj } from '@storybook/react'

import AddFundsPage from './AddFundsPage'

const meta: Meta<typeof AddFundsPage> = {
  component: AddFundsPage,
}

export default meta

type Story = StoryObj<typeof AddFundsPage>

export const Primary: Story = {}
q
